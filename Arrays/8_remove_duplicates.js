//Create a function that removes duplicate elements from an array

const removeDuplicates = (numbers) => {
    for (const number of numbers) {

    }
}

const res = removeDuplicates(1, 2, 21, 3, 24, 3, 5, 4, 6, 5, 4, 5, 3)

