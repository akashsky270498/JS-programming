//Create a variable radius and assign it a value. Calculate the area of a circle using the formula: 
// area = π * radius * radius.

"use strict"

const radius = 7;
const area = Math.PI * radius * radius;
console.log("Area of circle: ", area.toFixed(4)); // => Area of circle:  153.9380
// console.log(`Area of Circle is: ${Math.PI * radius * radius}`)